<!-- @format -->

# 实施进度记录

用于记录已完成的实施计划步骤。

## 阶段一：项目初始化与基础设施建设

- [x] **2026-04-01** **步骤 1.1：初始化核心框架** - 使用 Vite 创建 `frontend` 目录，配置 Vue 3 + TypeScript 模板，并成功运行本地开发服务器。
- [x] **2026-04-01** **步骤 1.2：安装并配置 UI 组件库** - 成功集成 Element Plus 及其按需引入插件，并在入口组件完成渲染验证。
- [x] **2026-04-01** **步骤 1.3：安装并配置样式方案** - 成功集成 Tailwind CSS 与 SCSS，利用 `@fontsource` 实现了 Noto Serif SC 的本地化加载，并配置了“数字胶片”主题色。
- [x] **2026-04-07** **步骤 1.4：配置插件化网络请求层** - 成功安装 Axios，采用插件化架构（Plugin-based）重构网络层，实现了独立的防抖去重、超时重试、鉴权（Session-Cookie）和错误处理插件，并在入口组件完成验证。

## 阶段二：状态管理与全局静态资源

- [x] **2026-04-07** **步骤 2.1：初始化路由和状态管理** - 安装 Vue Router 4 和 Pinia，创建了基础路由实例并挂载，配置了 UserStore、SpaceStore 和 PictureStore 空壳文件，并在 DevTools 中验证挂载成功。
- [x] **2026-04-07** **步骤 2.2：构建全局城市静态数据** - 创建 `src/constant/city.ts`，写入了全国地级市静态数组，并在首页组件引入，成功在页面打印显示数组长度（验证通过）。
- [x] **2026-04-07** **步骤 2.3：构建全局团队故事 Mock 数据** - 创建 `src/constant/team-story.ts`，按照规定结构定义了静态对象数组，并在入口组件控制台成功打印其结构（验证通过）。
- [x] **2026-04-07** **步骤 2.4：实现全局图片懒加载自定义指令** - 编写 `v-image-pro`，利用 IntersectionObserver 实现懒加载、`decoding="async"` 以及基于错误事件的重试和默认兜底图逻辑。已在占位首页通过 50 张长列表及 1 张模拟错图完成功能验证。

## 阶段三：身份认证、核心布局与路由守卫

- [x] **2026-04-01** 创建 `instance` 目录并完成 5 个核心页面的静态 HTML/Tailwind 原型设计（布局、个人、团队、公共图库、管理）。
- [x] **2026-04-01** 将“数字胶片”与“现代极简”设计规范融入架构文档与实施计划中。
- [x] **2026-04-01** 确立全局 UI 视觉参考规范，指定 `instance/screenshots` 作为 UI 开发的绝对参考基准。
- [x] **2026-04-08** **步骤 3.1：实现登录与注册页面** - 创建 `UserLoginPage.vue` 和 `UserRegisterPage.vue`，采用“数字胶片”极简风格。实现了 Element Plus 表单及账号密码长度与格式的校验逻辑。并在路由中配置便于验证。
- [x] **2026-04-09** **步骤 3.2前置准备：自动生成 API 接口代码** - 解决并记录了 `@umijs/openapi` 工具的 `tslib` 缺失及 JSON 解析报错问题。成功配置 `openapi.config.js`，运行脚本并在 `src/api` 目录下自动生成了所有带有 TypeScript 强类型的后端控制器请求方法（如 `userController` 等），并对生成文件中的 `@` 别名路径进行了编辑器提示的适配。
- [x] **2026-04-12** **步骤 3.2：集成状态保持与请求鉴权** - 修改了实施计划中的指令以匹配自动生成的 API。在 `UserLoginPage.vue` 中调用了 `frontend/src/api/userController.ts` 的登录 API，并在登录成功（code === 0）时通过 `frontend/src/store/modules/user.ts` 的 `setUserInfo` 存储用户状态。使用真实测试账号（wsb01）完成了登录并跳转至首页的验证。修复了由于 `dedupe` 插件防抖导致的 `CancelError` 穿透至业务层的报错异常，增加了业务层的 `isCancel` 兜底过滤。
- [x] **步骤 3.3：搭建页面基础路由与骨架屏** - 实现个人/团队/图库/管理页的基础组件与 el-skeleton 占位
- [x] **步骤 3.4：实现全局布局结构与动态菜单** - 编辑/杂志风格、动态侧边导航（基于路由 meta 和用户角色过滤）
- [x] **步骤 3.5：实现基于 Meta 的路由鉴权守卫** - 引入 pinia-plugin-persistedstate 实现状态持久化，结合路由 meta 拦截无权限访问并重定向
